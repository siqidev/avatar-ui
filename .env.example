# --- APIキー ---
# xAI Grok APIキー（必須）
XAI_API_KEY=
# Core APIへのアクセスキー（任意の文字列で自作・必須）
AVATAR_API_KEY=

# --- 接続先 ---
# Core APIエンドポイント（通常はローカル）
AVATAR_CORE_URL=http://127.0.0.1:8000/v1/think

# --- 実行環境 ---
# 使用するシェルを明示したい場合のみ設定（例: /bin/zsh, /bin/bash, pwsh）
AVATAR_SHELL=
# 作業ディレクトリ。未設定なら ~/Avatar/space
AVATAR_SPACE=
